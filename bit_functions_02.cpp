#include <bit>
#include <bitset>


int main()
{
    int x{35};

    //auto y = std::rotl(x); //invalid
    //auto n = std::popcount(x); //invalid
    //auto b = std::popcount(std::bitset<16>{3u}); //invalid
}
